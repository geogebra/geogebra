package org.geogebra.common.exam;

public enum ExamState {
	/** No exam is currently active. */
	INACTIVE,
	/** An exam is active. */
	ACTIVE,
	/** The exam summary (final log) is currently shown. */
	SUMMARY;
}
