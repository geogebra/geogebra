package geogebra.common.kernel;

/**
 * Only to be included in Algos such that:
 * - Are locusequable (or can be)
 * - The produce EquationElement is an EquationRestriction.
 * @author sergio
 *
 */
public interface RestrictionAlgoForLocusEquation {
	// Look, ma, I'm an empty interface.
}
