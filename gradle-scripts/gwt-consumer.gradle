configurations {
    gwtModule
}

task prepareGwtModules(type: Copy) {
    into "$buildDir/gwtModule/dist"
    from configurations.gwtModule
}

task unzipGwtModules(type: Copy) {
    from zipTree(prepareGwtModules)
    into "$buildDir/gwtModule/exploded"
}