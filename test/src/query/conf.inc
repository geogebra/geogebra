<?php

// Number of revisions to show at the same time as maximum:
$maxrevs=5;
// SQLite3 database file, preferably a symbolic link to
// the appropriate file (read-only access is enough):
$dbfile='sqlite3db';

?>