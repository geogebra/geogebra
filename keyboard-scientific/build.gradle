plugins {
     id 'java-library'
     id 'pmd'
     id 'checkstyle'
}

dependencies {
    api project(':keyboard-base')

    testImplementation libs.junit
}

pmd {
    ignoreFailures = true
    ruleSets = []
    ruleSetFiles = files("../config/pmd.xml")
}
